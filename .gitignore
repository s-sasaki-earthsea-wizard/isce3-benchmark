# Machine-dependent / large artifacts
data/*
!data/README.md
!data/.gitkeep

logs_*/
*.log
*.nsys-rep
*.qdrep
*.sqlite
*.csv
*.json
!reports/**/*.csv
!reports/**/*.json
!artifacts/polyfit-membership-20260811/*.json
!artifacts/insar-timing-20260810/*.json
!artifacts/polyfit-mitigation-20260819/*.json
# Closure-network manifest: the input nisar-displacement's closure_network.py
# and the MintPy handoff both reference by path, so it must be reproducible
# from a clone, not just regenerable by `make alos2-network`.
!configs/alos2_kujukuri/pairs_ALOS2_kujukuri.json

# Local environment overrides
.env
*.local.sh

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# Build artifacts (host-side; container uses /workspace)
build/
isce3-build/
isce3-install/

# Rendered S1 CSLC runconfigs (regenerated by `make render-s1` from templates).
# Templates and render tool are tracked; the rendered files contain a baked
# burst_id selection, so committing them would create churn on every regen.
configs/insar_s1_boso_cslc_cpu.yaml
configs/insar_s1_boso_cslc_gpu.yaml
configs/insar_s1_boso_cslc_sec_cpu.yaml
configs/insar_s1_boso_cslc_sec_gpu.yaml

# PoC build artifacts
poc/geocode_slc/geocode_slc_poc

# Local from-source isce3 build for the v0.25.16 tree (issue #32 reruns)
isce3-build-v0.25.16/

# SICD XML pretty-prints are derived from the tracked .sicd.xml
artifacts/sicd_headers/*.pretty.xml
